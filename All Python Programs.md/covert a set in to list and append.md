```
my_set = {1,2,3}
convert_to_list = list(my_set)
append_in_list = convert_to_list + [4,5]
print("the list after appending is:", append_in_list)
```
```
Output:
the list after appending is: [1, 2, 3, 4, 5]
```
