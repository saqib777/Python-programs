```
data = {"name": "saqib", "age" : 23, "role" : "SDET" }

turn_list = list(data.keys())
print("the list of dictionary keys is:", turn_list)
```

```
output:
The list of dictionary keys is: ['name', 'age', 'role']
```
`this is a demo line`
